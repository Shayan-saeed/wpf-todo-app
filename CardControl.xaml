<UserControl x:Class="WPFDemo.CardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFDemo"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="{Binding CardBackground, RelativeSource={RelativeSource AncestorType=UserControl}}" 
            CornerRadius="12" 
            Padding="16">
        <StackPanel>
            <TextBlock
                Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Foreground="{Binding FontColor, RelativeSource={RelativeSource AncestorType=UserControl}}"
                FontSize="18"
                FontWeight="Bold"
                Margin="0,0,0,10"/>

            <TextBlock Text="Line 1" Foreground="{Binding FontColor, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            <TextBlock Text="Line 2" Foreground="{Binding FontColor, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            <TextBlock Text="Line 3" Foreground="{Binding FontColor, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        </StackPanel>
    </Border>
</UserControl>
